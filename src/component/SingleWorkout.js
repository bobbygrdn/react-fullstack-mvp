const SingleWorkout = (props) => {
    return (
        <div className="singleWorkout">
            <h2 className="singleWorkoutInfo">{props.exercise_name}</h2>
        </div>
    )
}

export default SingleWorkout